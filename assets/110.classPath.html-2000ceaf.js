import{_ as e,p as a,q as c,a1 as s}from"./framework-c8e009f3.js";const o={},t=s(`<h2 id="classpath" tabindex="-1"><a class="header-anchor" href="#classpath" aria-hidden="true">#</a> classPath</h2><p>java是一个以类为基础的语言，那么我们要如何在一个class中去获取另一个class呢，答案是<code>classpath</code>：</p><p><code>classpath</code> 是jvm运行时用到的一个环境变量，指示jvm如何搜索<strong>class</strong>。是一组目录的集合，在windows和linux上写法不同：</p><p>windows上用<code>;</code>分隔，带空格的目录有<code>&quot;&quot;</code>引号，可以是相对路径:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>.;C:\\work\\project1\\bin;C:\\shared;&quot;D:\\My Documents\\project1\\bin&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>在windows中， <code>cmd</code> 执行语句的分隔符是 <strong>&amp;</strong> , 而 <code>powerShell</code> 执行语句的分隔符是 <strong>;</strong></p><p>如果你使用的命令行工具是<code>powerShell</code>并且想用分号*;* 来分割多个classpath的话，需要使用 反引号**\`;**来转义分号</p></div><p>linux上用<code>:</code>分隔：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>shared:/usr/local/bin:/home/liaoxuefeng/bin
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>举个更真实的例子：jvm加载一个类<code>abc.xyz.Hello</code>，它获取到的classpath是<code>.;C:\\work\\project1\\bin;C:\\shared</code>,它会依次查找：</p><ul><li><code>.</code> : 当前目录：<em>\\abc\\xyz\\Hello.class</em>（无指定classpath时默认）</li><li><em>C:\\work\\project1\\bin\\abc\\xyz\\Hello.class</em></li><li><em>C:\\shared\\abc\\xyz\\Hello.class</em></li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>包名<code>abc.xyz.hello</code> 会被自动解析成路径： <code>abc/xyz/Hello.class</code></p></div><p><strong>设定classpath：</strong></p><p>运行<code>java</code>命令时传入：<code>java -classpath 路径集合 目标类</code>，其中-classpath可以用 <code>-cp</code>简写：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>java -cp .;C:\\work\\project1\\bin;C:\\shared abc.xyz.Hello
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>在IDE中，通常IDE会自动传入-cp参数：bin目录和引入的jar包</p></div>`,15),l=[t];function d(i,n){return a(),c("div",null,l)}const p=e(o,[["render",d],["__file","110.classPath.html.vue"]]);export{p as default};
